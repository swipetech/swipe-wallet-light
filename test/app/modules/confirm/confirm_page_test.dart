import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:swipe_app/app/modules/confirm/confirm_page.dart';

void main() {
  testWidgets('ConfirmPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ConfirmPage(title: 'Confirm')));
    //  final titleFinder = find.text('Confirm');
    //  expect(titleFinder, findsOneWidget);
  });
}
