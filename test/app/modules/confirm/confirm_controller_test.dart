import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:swipe_app/app/modules/confirm/confirm_controller.dart';
import 'package:swipe_app/app/modules/confirm/confirm_module.dart';

void main() {
  initModule(ConfirmModule());
  // ConfirmController confirm;
  //
  setUp(() {
    //     confirm = ConfirmModule.to.get<ConfirmController>();
  });

  group('ConfirmController Test', () {
    //   test("First Test", () {
    //     expect(confirm, isInstanceOf<ConfirmController>());
    //   });

    //   test("Set Value", () {
    //     expect(confirm.value, equals(0));
    //     confirm.increment();
    //     expect(confirm.value, equals(1));
    //   });
  });
}
